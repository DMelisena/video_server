# Core ML libraries (consider CPU versions if GPU not needed)
tensorflow-cpu==2.20.0  # Use CPU version if no GPU needed
torch==2.8.0+cpu  # Use CPU version if no GPU needed
torchvision==0.23.0+cpu  # Use CPU version if no GPU needed

# Essential data science
numpy==2.2.6
pandas==2.3.2
scikit-learn==1.7.1
scipy==1.16.1

# Visualization
matplotlib==3.10.5
seaborn==0.13.2

# Computer Vision
opencv-python-headless==4.12.0.88  # Headless version is smaller

# Web framework
Flask==3.1.2
Jinja2==3.1.6
Werkzeug==3.1.3

# YOLO/Ultralytics
ultralytics==8.3.183
ultralytics-thop==2.0.16

# Essential utilities
requests==2.32.5
tqdm==4.67.1
PyYAML==6.0.2
pillow==11.3.0

# Remove if not essential:
# keras==3.11.2  # Already included in TensorFlow
# tensorboard==2.20.0  # Only if you need it
# rich==14.1.0  # Only if you use rich formatting
